# AION EMS Zeus v14.0.0-alpha.172 — Runtime Helper Fix Release

Cumulative from the user-confirmed working baseline `14.0.0-alpha.170`.

Fixes carried into the public GitHub/HACS release:
- Statistics runtime error: `isSourceOrGenerationDevice is not a function`
- Memory runtime error: `analyticsDeviceRows is not a function`

Both helpers are restored as component class methods.

No UI redesign, energy authority, finance logic, intelligence, learning,
control, Scheduler, DEA, Forecast, Copilot, Recorder or persistence changes.

Development baseline remains `14.0.0-alpha.170`.
This `.172` package is the public update release carrying the confirmed fixes.
