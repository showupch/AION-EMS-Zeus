# Technical Guide — v11.7.4

Zeus is a Home Assistant native custom integration with storage-backed registry data, recorder-safe summary sensors, modular engines, and a custom frontend panel. Heavy animation is visibility-aware and motion preferences are applied through `data-motion`. Runtime diagnostics are local browser measurements only.

Core guarantees: incremental processing, no autonomous control, multi-inverter support, and preserved Registry mappings during upgrades.
